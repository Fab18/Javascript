/*
  Gatti in fila
  Scrivi un programma che prenda in input:
    - Un numero totale di gatti
    - Il numero dei gatti presenti in ogni fila
  Restituisca in output:
   - Il numero di file risultantinon usare Math.round (arrotonda all'intero superiore solo da .5 in su) ma utilizza Math.ceil (arrotonda sempre all'intero superiore).
  http://www.imparareaprogrammare.it
*/



var gatti = 44;
var fila = 6;

parseInt(44);
parseInt(6);

console.log(Math.ceil(gatti/fila));

var tot = (Math.ceil(gatti/fila)*(fila));

parseInt(Math.ceil(gatti/fila)*(fila));

console.log(tot-gatti);
